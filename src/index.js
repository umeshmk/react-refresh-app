import React from 'react';
import ReactDOM from 'react-dom';

import './index.css'; // always before <App/> component's css
import App from './components/App';

ReactDOM.render(<App />, document.getElementById('root'));
